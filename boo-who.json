function booWho(bool) {
  if (bool === true) {
    return true  
  }
  else if (bool == false) {
    return true  
  }  
  return false;
  }
}

//CHECKS IF ARGUMENT IS A FALSE VALUE

//EXAMPLE

booWho(1); //FALSE
booWho(null); //TRUE
